#include <iostream>

int arr[5];
int temp = 0;

int accumulate (int*first, int*last, int temp)
{
for ( , first != last; ++first)
    temp = sum (temp, *first)
}

int sum (int*arr, int first, int last, int temp)
{
    for (int first = 10; firs < last; ++first)
    {
        temp += arr[first]
    }
    return temp;
}
{
    arr[0]
}
int main()
{
    return 0;
}

